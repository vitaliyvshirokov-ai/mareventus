@extends('layouts.admin') @section('title',$service->exists?'Edit Service':'Add Service') @section('content')<form class="max-w-3xl space-y-6 bg-white p-8 shadow-sm" method="POST" action="{{ $service->exists?route('admin.services.update',$service):route('admin.services.store') }}">@csrf @if($service->exists)@method('PUT')@endif<label class="block text-sm font-bold text-navy">Title<input class="mt-2 w-full border-slate-300" name="title" value="{{ old('title',$service->title) }}" required></label><label class="block text-sm font-bold text-navy">Description<textarea class="mt-2 w-full border-slate-300" name="description" rows="6" required>{{ old('description',$service->description) }}</textarea></label><div class="grid gap-5 sm:grid-cols-2"><label class="block text-sm font-bold text-navy">Icon<select class="mt-2 w-full border-slate-300" name="icon">@foreach(['route','ship','drop','chart'] as $icon)<option value="{{ $icon }}" @selected(old('icon',$service->icon)===$icon)>{{ ucfirst($icon) }}</option>@endforeach</select></label><label class="block text-sm font-bold text-navy">Sort order<input class="mt-2 w-full border-slate-300" type="number" name="sort_order" value="{{ old('sort_order',$service->sort_order??0) }}" required></label></div><button class="button-primary">Save service</button></form>@endsection
