@extends('layouts.admin') @section('title','Overview') @section('content')<div class="grid gap-5 sm:grid-cols-2 xl:grid-cols-4">@foreach(['services'=>'Services','fleet'=>'Fleet images','partners'=>'Partners','open_requests'=>'Open enquiries'] as $key=>$label)<div class="bg-white p-6 shadow-sm"><strong class="text-4xl font-extrabold text-navy">{{ $counts[$key] }}</strong><p class="mt-2 text-sm text-slate-500">{{ $label }}</p></div>@endforeach</div><div class="mt-8 bg-white p-6 shadow-sm"><h2 class="text-xl font-bold text-navy">Recent enquiries</h2><div class="mt-5 divide-y divide-slate-100">@forelse($recent as $item)<a href="{{ route('admin.contacts.show',$item) }}" class="flex items-center justify-between py-4"><span><strong class="block text-navy">{{ $item->name }}</strong><small class="text-slate-500">{{ $item->company ?: $item->email }}</small></span><span class="text-xs text-slate-400">{{ $item->created_at->format('d M Y, H:i') }}</span></a>@empty<p class="py-6 text-slate-500">No enquiries yet.</p>@endforelse</div></div>@endsection
