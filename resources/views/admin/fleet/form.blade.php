@extends('layouts.admin') @section('title',$fleet->exists?'Edit Fleet Image':'Upload Fleet Image') @section('content')<form class="max-w-3xl space-y-6 bg-white p-8 shadow-sm" enctype="multipart/form-data" method="POST" action="{{ $fleet->exists?route('admin.fleet.update',$fleet):route('admin.fleet.store') }}">@csrf @if($fleet->exists)@method('PUT')@endif<label class="block text-sm font-bold text-navy">Image {{ $fleet->exists?'(optional)':'*' }}<input class="mt-2 block w-full text-sm" type="file" name="image" accept="image/jpeg,image/png,image/webp" @required(!$fleet->exists)><span class="mt-2 block text-xs font-normal text-slate-500">Upload only neutral imagery without flags, vessel names, operator marks or watermarks. Files are automatically renamed.</span></label><label class="block text-sm font-bold text-navy">English caption<input class="mt-2 w-full border-slate-300" name="caption" value="{{ old('caption',$fleet->caption) }}" required></label><label class="block text-sm font-bold text-navy">Sort order<input class="mt-2 w-full border-slate-300" type="number" name="sort_order" value="{{ old('sort_order',$fleet->sort_order??0) }}" required></label><button class="button-primary">Save image</button></form>@endsection
