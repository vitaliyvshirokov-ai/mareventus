@extends('layouts.admin') @section('title','Fleet Gallery') @section('content')<div class="mb-6 flex justify-end"><a class="button-primary" href="{{ route('admin.fleet.create') }}">Upload image</a></div><div class="grid gap-6 md:grid-cols-2 xl:grid-cols-3">@foreach($items as $item)<article class="bg-white p-4 shadow-sm"><img class="h-48 w-full object-cover" src="{{ str_starts_with($item->path,'assets/')?asset($item->path):Storage::url($item->path) }}" alt="{{ $item->caption }}"><p class="mt-4 font-bold text-navy">{{ $item->caption }}</p><p class="mt-1 text-xs text-slate-500">Order {{ $item->sort_order }}</p><div class="mt-4 flex gap-4"><a class="font-bold text-orange" href="{{ route('admin.fleet.edit',$item) }}">Edit</a><form method="POST" action="{{ route('admin.fleet.destroy',$item) }}" onsubmit="return confirm('Delete this image?')">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></div></article>@endforeach</div>@endsection
