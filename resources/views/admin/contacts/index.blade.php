@extends('layouts.admin') @section('title','Enquiries') @section('content')<div class="overflow-hidden bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="p-4">Contact</th><th class="p-4">Company</th><th class="p-4">Date</th><th class="p-4">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($items as $item)<tr><td class="p-4"><a class="font-bold text-navy" href="{{ route('admin.contacts.show',$item) }}">{{ $item->name }}</a><small class="block text-slate-500">{{ $item->email }}</small></td><td class="p-4">{{ $item->company ?: '—' }}</td><td class="p-4">{{ $item->created_at->format('d M Y, H:i') }}</td><td class="p-4"><span class="{{ $item->is_processed?'text-emerald-700':'text-orange' }}">{{ $item->is_processed?'Processed':'Open' }}</span></td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $items->links() }}</div>@endsection
