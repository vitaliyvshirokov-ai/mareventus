@extends('layouts.admin') @section('title','Enquiry Details') @section('content')<article class="max-w-3xl bg-white p-8 shadow-sm"><div class="grid gap-5 sm:grid-cols-2"><div><span class="text-xs uppercase text-slate-400">Name</span><p class="font-bold text-navy">{{ $contact->name }}</p></div><div><span class="text-xs uppercase text-slate-400">Company</span><p>{{ $contact->company ?: 'Not provided' }}</p></div><div><span class="text-xs uppercase text-slate-400">Email</span><p><a class="text-orange" href="mailto:{{ $contact->email }}">{{ $contact->email }}</a></p></div><div><span class="text-xs uppercase text-slate-400">Phone</span><p>{{ $contact->phone ?: 'Not provided' }}</p></div></div><div class="mt-7 border-t border-slate-100 pt-7"><span class="text-xs uppercase text-slate-400">Message</span><p class="mt-2 whitespace-pre-line leading-7">{{ $contact->message }}</p></div><form class="mt-8" method="POST" action="{{ route('admin.contacts.update',$contact) }}">@csrf @method('PATCH')<button class="button-primary">Mark as {{ $contact->is_processed?'open':'processed' }}</button></form></article>@endsection
